import React from "react";
import ReactDOM from "react-dom/client";
import SenateMagicWall from "./SenateMagicWall";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <SenateMagicWall />
  </React.StrictMode>
);
