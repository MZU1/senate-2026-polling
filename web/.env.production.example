# Copy to .env.production (gitignored) for a local production build, or set this directly as
# a Render Static Site build-time environment variable -- Vite inlines it at build time, so it
# must be set BEFORE `npm run build` runs, not read at runtime.
VITE_POLLING_API_BASE=https://senate-2026-polling-api.onrender.com
